name: IndexNow discovery ping

on:
  push:
    branches: [main]
  workflow_dispatch:

# Exposure-normalization resubmit: 2026-09-04
permissions:
  contents: write

concurrency:
  group: indexnow-main
  cancel-in-progress: true

jobs:
  notify:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Ensure search engine verification meta
        run: node tools/patch-site-verification.mjs
      - name: Commit verification meta if needed
        shell: bash
        run: |
          if git diff --quiet -- index.html; then
            echo "SITE_VERIFICATION_COMMIT=not-needed"
          else
            git config user.name "programmer119"
            git config user.email "53187583+programmer119@users.noreply.github.com"
            git add index.html
            git commit -m "Add search engine verification meta"
            git push
          fi
      - name: Submit current discovery URLs
        run: node tools/indexnow-submit.mjs
