(()=>{window.SF_ANALYTICS?.event('diagnosis_page_view');const form=document.getElementById('diagForm'),status=document.getElementById('status'),progress=document.getElementById('progress'),btn=document.getElementById('startBtn');const preset=new URLSearchParams(location.search).get('url');if(preset){const field=document.getElementById('url');if(field)field.value=preset;}form.addEventListener('submit',async e=>{e.preventDefault();status.className='status';status.textContent='주소를 확인하고 진단 작업을 준비하고 있습니다.';progress.classList.add('on');btn.disabled=true;const url=document.getElementById('url').value.trim();try{const a=window.SF_ANALYTICS;const r=await fetch(a.API+'/api/v1/diagnoses',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({...a.touch,url})});const b=await r.json();if(!r.ok)throw new Error(b.error||'진단을 시작하지 못했습니다.');location.href='./result/?id='+encodeURIComponent(b.diagnosisId);}catch(err){progress.classList.remove('on');btn.disabled=false;status.className='status error';status.textContent=err.message||String(err)}})})();
